const Group = require('../models/group.model')
const createGroup = (args) => {
  Group.insertMany[{ name: 'Primer nombre para testear', description: 'mi descripcion', category: 'categoria unoooo'}]
  return
}